import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReferringProvider5010d2310aComponent } from './referring-provider-5010d-2310a.component';

describe('ReferringProvider5010d2310aComponent', () => {
  let component: ReferringProvider5010d2310aComponent;
  let fixture: ComponentFixture<ReferringProvider5010d2310aComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReferringProvider5010d2310aComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReferringProvider5010d2310aComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
